sudo: required
language: c
compiler: gcc
dist: trusty
addons:
  apt:
    packages:
    - autoconf
    - automake
    - autotools-dev
    - aclocal
    - intltool
    - libgstreamer1.0-dev
    - libgstreamer-plugins-base1.0-dev
    - libgtk-3-dev
    - gtk-doc-tools
script: 
 - ./configure && make
